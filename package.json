{
  "name": "pi-agent-monorepo",
  "version": "1.0.0",
  "packageManager": "bun@1.2.14",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "start": "turbo run start"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.8.17"
  }
}
